// Migrated from next.config.ts to next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add valid Next.js config options
    // output: 'export',           // ← This is the key line
    // images: {
    //   unoptimized: true,        // Required because Next Image optimization needs a server
    // },
    // trailingSlash: true,        // Recommended for better routing in Capacitor
};


export default nextConfig;
